<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="busservice.dao.IBusStopDAO">
    <resultMap id="BusStopMap"
               type="BusStop"
               autoMapping="false">
        <id property="id" column="id"/>
        <result property="longitude" column="longitude"/>
        <result property="latitude" column="latitude"/>
        <result property="name" column="stop_name"/>
        <result property="isTerminal" column="is_terminal"/>
        <association property="city"
                     javaType="City"
                     resultMap="busservice.dao.ICityDAO.CityMap"/>
        <collection property="buses"
                    javaType="ArrayList"
                    ofType="Bus"
                    resultMap="busservice.dao.IBusDAO.BusMap"/>
    </resultMap>

    <select id="getById" parameterType="int" resultMap="BusStopMap">
        SELECT *
        FROM bus_stop bs
            INNER JOIN city ON bs.city_id = city.id
            INNER JOIN route ON route.stop_id = bs.id
            INNER JOIN bus b on route.bus_id = b.id
        WHERE bs.id = #{id};
    </select>

    <select id="getAll" resultMap="BusStopMap">
        SELECT *
        FROM bus_stop bs
            INNER JOIN city ON bs.city_id = city.id
            INNER JOIN route ON route.stop_id = bs.id
            INNER JOIN bus b on route.bus_id = b.id
        ORDER BY bs.id;
    </select>

    <insert id="insert" parameterType="BusStop">
        INSERT INTO bus_stop (longitude, latitude, stop_name, is_terminal, city_id)
        VALUES (#{busStop.longitude}, #{busStop.latitude}, #{busStop.name}, #{busStop.isTerminal}, #{busStop.city.id});
    </insert>

    <update id="update" parameterType="BusStop">
        UPDATE bus_stop
        SET
            longitude = #{busStop.longitude},
            latitude = #{busStop.latitude},
            stop_name = #{busStop.name},
            is_terminal = #{busStop.isTerminal},
            city_id = #{busStop.city.id}
        WHERE id = #{busStop.id};
    </update>

    <delete id="deleteById" parameterType="int">
        DELETE
        FROM bus_stop
        WHERE id = #{id}
    </delete>

</mapper>
